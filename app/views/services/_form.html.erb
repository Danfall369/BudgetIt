  <%= form_with(model: service, html: { class: 'Add-service' }) do |form| %>
    <% if service.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(service.errors.count, "error") %> prohibited this service from being saved:</h2>
  
        <ul>
          <% service.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.text_field :name, placeholder: "Service Name" %>
    </div>
  
    <div class="field">
      <%= form.text_field :icon, placeholder: "Service Icon" %>
    </div>
  
    <div class="actions">
      <%= form.submit "Save" %>
    </div>
  <% end %>