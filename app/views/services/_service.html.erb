<div id="<%= dom_id service %>">

  <p>
    <strong>Name:</strong>
    <%= service.name %>
  </p>

  <p>
    <strong>Icon:</strong>
    <img src="<%= service.icon %>" alt="<%= service.name %>" />
  </p>

</div>
